<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="css/addremove.css" />
    <title>Register</title>
  </head>
  <body>
    <div id="grid-container">
      <div id="left">
        <div class="header">
          <h1 class="logo">GameList</h1>
        </div>
         <!-- Loop through errors array and display error, if any -->
        {{#each errors}}
            <div class="error">
                <p>{{text}}</p>
            </div>
        {{/each}}
         <!-- Get and display success message, if any -->
        {{#each success}}
            <div class="success">
                <p>{{text}}</p>
            </div>
        {{/each}}
        <div class="form">
          <form action="/add" method="POST" autocomplete="off">
            <div>
              <label for="title">Game Title</label>
              <input type="text" name="title" id="title" class="text-input" value={{title}}>
            </div>
            <button type="submit" class="btn btn-left">Add</button>
          </form>
        </div>
        <div class="show-on-small">
          <p>View games instead</p>
          <div class="games">
            <a class="btn btn-signin" href="/user">Games</a>
          </div>
          <p>Remove game instead</p>
          <div class="signin">
            <a class="btn btn-signin" href="/remove">Remove</a>
          </div>
        </div>
      </div>
      <div id="right" class="hide-on-small">
        <div class="right-content">
          <p>View games instead</p>
          <div class="games">
            <a class="btn btn-signin" href="/user">Games</a>
          </div>
          <p>Remove game instead</p>
          <div class="signin">
            <a class="btn btn-signin" href="/remove">Remove</a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
