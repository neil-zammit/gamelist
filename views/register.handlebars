<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="css/auth.css" />
    <title>Register</title>
  </head>
  <body>
    <div id="grid-container">
      <div id="left">
        <div class="header">
          <h1 class="logo">GameList</h1>
        </div>
         <!-- Loop through errors array and display error, if any -->
        {{#each errors}}
            <div class="error">
                <p>{{text}}</p>
            </div>
        {{/each}}
        <div class="form">
          <form
            action="/register"
            method="POST"
            class="register-form"
            autocomplete="off"
          >
            <div>
              <label for="email">Email</label>
              <input type="text" id="email" name="email" class="text-input" value={{email}}>
            </div>
            <div>
              <label for="password">Password</label>
              <input
                type="password"
                id="password"
                name="password"
                class="text-input"
                value={{password}}>
            </div>
            <div>
              <label for="password"> Confirm Password</label>
              <input
                type="password"
                id="password2"
                name="password2"
                class="text-input"
                value={{password2}}>
            </div>
            <button type="submit" class="btn btn-left">Register</button>
          </form>
        </div>
        <div class="show-on-small">
          <p>Already have an account?</p>
          <p>Sign In instead</p>
          <div class="signin">
            <a class="btn btn-signin" href="/signin">Sign In</a>
          </div>
        </div>
      </div>
      <div id="right" class="hide-on-small">
        <div class="right-content">
          <p>Already have an account?</p>
          <p>Sign In instead</p>
          <div class="signin">
            <a class="btn btn-signin" href="/signin">Sign In</a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
